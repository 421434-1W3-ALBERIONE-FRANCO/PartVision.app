# Flutter/Dart
.dart_tool/
.packages
build/
.flutter-plugins
.flutter-plugins-dependencies
pubspec.lock
*.iml
.idea/
.vscode/

# Nota: las carpetas de plataforma (android/, web/, windows/) SÍ están versionadas
# (se commitearon con el scaffold). Si preferís regenerarlas en vez de versionarlas,
# ignoralas acá y borralas del índice con `git rm -r --cached`. Ver README.
